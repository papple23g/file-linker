# Local publish token note template
#
# Copy this file outside the repository before adding real tokens.
# Recommended location:
#   <repo path> note.txt
#
# Do not put real tokens in this file while it is inside the repository.

# VS Marketplace:
# Prefer `vsce login peterwang` so the token is stored by vsce.
# If needed for automation, use the current PowerShell session only:
# VSCE_PAT = ""

# Open VSX:
# OVSX_TOKEN = ""
